//
//  SupabaseSecrets.swift.example
//  Finance buddy
//
//  Template for the git-ignored SupabaseSecrets.swift.
//  Copy this file to `SupabaseSecrets.swift` (same folder) and fill in the
//  values from Supabase dashboard → Project Settings → API.
//  The real file is listed in .gitignore and must never be committed.
//

import Foundation

enum SupabaseSecrets {
    static let projectURL = URL(string: "https://YOUR-PROJECT.supabase.co")!
    static let anonKey = "YOUR-ANON-PUBLISHABLE-KEY"
}
